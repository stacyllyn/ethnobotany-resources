```{r echo=TRUE, message=FALSE}
library(readr)
library(knitr)

# Check current directory
cat("Current directory:", getwd(), "\n\n")

# List CSV files in current directory
cat("CSV files in current directory:\n")
print(list.files(pattern = "*.csv"))

# List CSV files in parent directory
cat("\nCSV files in parent directory:\n")
print(list.files("..", pattern = "*.csv"))

# List CSV files in main project folder
cat("\nCSV files in main project folder:\n")
print(list.files("C:/Users/Stacy/OneDrive/Desktop/ethnobotany-resources2", pattern = "*.csv"))

              

